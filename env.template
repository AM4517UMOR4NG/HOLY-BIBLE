# ========================================
# HOLY BIBLE - Environment Variables
# ========================================
# Copy file ini ke .env dan sesuaikan nilai-nilainya

# ========================================
# DATABASE
# ========================================
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://hb_user:hb_pass@localhost:5432/holybible

# ========================================
# REDIS (Optional - untuk background jobs)
# ========================================
# Redis connection URL
# Jika tidak ada Redis, backend tetap berjalan tapi fitur job queue tidak aktif
REDIS_URL=redis://localhost:6379

# ========================================
# MEILISEARCH (Optional - untuk search)
# ========================================
# Meilisearch server URL dan API key
MEILI_HOST=http://localhost:7700
MEILI_API_KEY=masterKey

# ========================================
# MINIO/S3 (Optional - untuk file upload)
# ========================================
# MinIO/S3 configuration
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
S3_BUCKET=holy-bible

# ========================================
# JWT AUTHENTICATION
# ========================================
# Secret key untuk JWT token (GANTI dengan random string untuk production!)
# Generate dengan: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_PRIVATE_KEY=dev_secret_change_me_in_production

# JWT token expiration (dalam detik)
JWT_EXPIRES_IN=3600

# Refresh token expiration
REFRESH_TOKEN_EXPIRES=30d

# ========================================
# SERVER
# ========================================
# Port untuk backend server
PORT=4000

# Environment mode
NODE_ENV=development

# ========================================
# CORS (Optional)
# ========================================
# Allowed origins untuk CORS (comma separated)
# Contoh: CORS_ORIGIN=https://yourapp.vercel.app,https://www.yourapp.com
# Kosongkan untuk development (allow all)
# CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# ========================================
# LOGGING (Optional)
# ========================================
# Log level: trace, debug, info, warn, error, fatal
# LOG_LEVEL=info

# ========================================
# CATATAN PENTING
# ========================================
# 1. Jangan commit file .env ke git!
# 2. Untuk production, ganti semua secret keys dengan nilai yang aman
# 3. Redis dan Meilisearch bersifat opsional untuk development
# 4. Pastikan PostgreSQL sudah berjalan sebelum start backend
